---
- name: "Task: Create Namespaces"
  kubernetes.core.k8s:
    kubeconfig: "{{ k3s_kubeconfig_path }}"
    name: "{{ item }}"
    api_version: v1
    kind: Namespace
    state: present
  loop: "{{ qai_namespaces }}"
